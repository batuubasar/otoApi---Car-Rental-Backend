spring:
  datasource:
    username: postgres
    password: batuhan
    url: jdbc:postgresql://localhost:5432/otocom
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: "batuhan"
    password: "batuhan"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
project:
  poster: posters/
base:
  url: "http://localhost:8080"